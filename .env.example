# Copy to .env (git-ignored). PLANTOD auto-loads it into the environment.
# Put here any env vars your chosen provider CLIs read for auth, e.g.:
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
